Just simply run the make command.

$ make

It will automatically generate an executable file named 'linker'. To use 'linker', just need to type:

$ linker input-1

It will then print out the output to screen.

Note: The require version of C++ is 4.8